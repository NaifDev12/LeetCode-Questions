class Solution {
    public String defangIPaddr(String address) {

        return address.replace(".","[.]");// uses the replace method which is inbuilt in the java compiler which is used to replace characters with required characters
      
        
    }
}
